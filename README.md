# javascript

## Variables (What? What!)

### Variables are THINGS, AND they change 

In Javascript, when we want to make A THING we use the keyword

## Var
and we tell the computer what thing the Var is, is by telling is
## = 
then WHAT the THING is 

do if you want to make a "dog", you tell the computer Var is a Dog

or

`var dog = "dog" `

`""` is a string variable

what do you the the number 1 is?

### Math symbols

#### +
#### -
#### *
#### /
#### >
#### <
#### == (===) is better but a little confusion

### Different types of Variables

#### string 
#### int
#### boolean (bool)

#### How comparisons of equality work

### int and int 
  anything is OK

### string and string 
  compares lenght of string as an int

## Core Logic & Control Flow (Immediate Next Step)
  
### Functions: Teach them that functions are like cooking recipes or LEGO instructions—reusable blocks of code that perform actions.

### Parameters: Show how to pass information into functions (e.g., bake("cookies") vs bake("cake")).

### Conditionals (if/else): Teach the computer to make decisions. "If score > 10, show 'You Win!' else 'Keep Trying'".Comparison Operators (===, !=, <, >): Essential for logical checks.

## FUNCTIONS

  if a Variable(var) is a THING, a Function(function) is a DO. Function let you DO. They are easy to use and fun to play with. Think of a BOX. A box that has one JOB to do. Every time you hit the box, something will come out.

#### What we did 2026-04-09
introduced functions as a box where
 1. you can call a function that will always produce an expected response (console.log())
 2. passing a parameter onto the function and access it withing the body. (relationship between the parameter name and how to reference it)
 3. passing multiple parameters. (how each parameter ownes a slot and if the parameters are mismatched, there are errors or undefined)
 
 We used the example as putting something (parameter) into a box and how to add space to add more things to the box (multi-parameters)


  ## Fun Stuff

  ### 3. Fun Activity: "The Robot Game"
Ask them to be the "Robot" and you be the "Coder."
*   **Define a Function:** Tell them, "I'm creating a function called `Jump` with a parameter called `times`."
*   **Call the Function:** Say, "Robot, `Jump(3)`!"
*   **Switch Roles:** Let them define a function for you (like `makeFace(expression)`) and "call" it with different arguments like "silly" or "scary."

### Next-Step Projects
Once they understand passing data into functions, they can try building:
*   **A "Greeting Generator":** A function that takes a `name` and `timeOfDay` to say "Good morning, [Name]!"
*   **A Simple Quiz:** Use their comparison skills within a function to check if an answer is correct.
*   **Interactive Tools:** Platforms like [CodeCombat](https://codecombat.com) or [Kid.js](https://kidjs.app/activities) are excellent for practicing functions in a game-like environment.


#### 2026-05-24

Let's move as fast as we can!! 

  - [x] HTML    - Start Button
  - [x] HTML    - Stop Button
  - [x] HTML    - Next Button
  - [x] HTML    - Stopwatch
  
  ----
###  2026-05-31
  - [x] JS      - Stopwatch Timer Event Start
  - [x] JS      - Accumumated Time
  - [x] JS/HTML - Good Job + Finish Time
  - [x] JS/HTML - Go! Indicator (if command is correct)
  - [x] JS     - Make Function
  - [ ] HTML    - (optional) Next Button

### 2026-06-14

#### Concepts:
  - Maths - Math.Random();
  - Arrays 
      - Basic
    - [x] push
    - [-] pop
    - [x] index
    - NOT YET: looping
      - Advanced?
        - [-] itemBag object with built in methods.
        - 
  Fix / improved game play for the Go Pomni and Pomni Maze games